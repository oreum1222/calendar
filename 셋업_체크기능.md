# [오름] 마감 캘린더 — 담당 체크 기능 셋업

데모로는 바로 동작하지만, **"누가 체크했는지"를 원장·모든 강사가 공유해서 보려면** 구글시트 백엔드를 한 번만 연결하면 됩니다.

## 1) 구글시트 만들기 (시트 4개)
새 구글시트 하나에 아래 탭 4개를 만들고 1행에 헤더를 입력합니다.

**① "일정"**

| 제목 | 마감일 | 카테고리 | 담당 | 상태 | 반복 | 메모 | 링크 |
|---|---|---|---|---|---|---|---|
| 기말 대비반 홍보글 | 2026-06-07 | 홍보 | 전체 | todo | 없음 | 반별 시험범위 녹여서 | |
| 2021 고2 모고 분석지 | 2026-06-04 | 모고 | 은규T, 지연T | todo | 없음 | 각 2개 | |

- **담당** = `전체`(= 강사 전원) 또는 `은규T, 지연T`처럼 쉼표로 명단
- 상태: `todo`/`doing`/`done` · 반복: `없음`/`매주`/`매월`

**② "강사"** — PIN은 원장만 알기

| 이름 | PIN |
|---|---|
| 가경T | 1212 |
| 은규T | 1111 |
| 지연T | 2222 |
| 다미T | 3333 |
| 해솔T | 4444 |
| 원철T | 5555 |

**③ "조교"** — 강사와 같은 형식의 별도 탭 (이유섭·오수아·김주은·백수정·김규민·김재희·장서윤·정경은·진채현 9명)

| 이름 | PIN |
|---|---|
| 이유섭 | 7001 |
| … | … |

- 조교는 `전체`에 포함되지 않음. 조교 업무는 일정 `담당` 칸에 **이름 직접** 또는 **`조교전체`** 로 배정.

**④ "체크"** — 헤더만 두면 됨(내용은 자동 기록)

| 제목 | 마감일 | 이름 | 완료 | 시각 |
|---|---|---|---|---|

## 2) Apps Script 붙이기
1. 그 시트에서 **확장 프로그램 → Apps Script**
2. `Code.gs` 파일 내용을 전부 붙여넣기(이 폴더의 Code.gs)
3. **배포 → 새 배포 → 유형 '웹 앱'**
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자**
4. **배포** 누르고 권한 승인 → 나오는 **웹 앱 URL(…/exec)** 복사

## 3) index.html 연결
`index.html` 상단 CONFIG에서 한 줄만:
```js
apiUrl: "여기에 2)에서 복사한 .../exec 주소",
```
- `apiUrl`을 넣으면 일정·체크 모두 시트에서 읽고, 체크는 시트에 저장됩니다.
- 비워두면 데모 모드(이 기기 임시 체크)로 동작합니다.

## 4) 사용법
- 화면 상단 **"나 설정"** → 본인 이름 + PIN → 세션 동안 본인으로 고정
- 각 카드의 담당자 칩 중 **내 칩만** 눌러 체크/해제 → 시트에 기록
- `2/4 완료`로 진행 표시, 안 한 사람은 회색으로 남음 → 원장은 누가 안 했는지 한눈에

> PIN·담당 명단은 "강사"·"일정" 시트에서 언제든 수정(코드 변경 없으면 재배포 불필요).
> 데스크·실장에게 시트 편집 권한을 주면 일정 입력도 위임됩니다.
